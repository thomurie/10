const PI = 3.14;
PI = 42; //errors out

/* What is the difference between var and let?
var can be redeclared
let is block scoped 

What is the difference between var and const?
var can be redeclared and redfined
const is constant you can not redeclare or modify the code
const is block scoped

What is the difference between let and const?
let can be reassigned, const cannot be reassinged

What is hoisting?
variables are hoisted to the scope of the block where they are defined in.
If working in that block you can call the variable before it is defined
and thanks to hoisting the coe will run. 

*/
